<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/entrant_signup_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="24dp">

    <TextView
        android:id="@+id/text_entrant_signup_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Entrant Sign Up"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="24dp" />

    <EditText
        android:id="@+id/edit_entrant_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Full name"
        android:layout_marginBottom="12dp" />

    <EditText
        android:id="@+id/edit_entrant_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:layout_marginBottom="12dp" />

    <EditText
        android:id="@+id/edit_entrant_phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Phone (optional)"
        android:inputType="phone"
        android:layout_marginBottom="24dp" />

    <Button
        android:id="@+id/button_entrant_save"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save and continue"
        android:layout_marginBottom="8dp" />

    <Button
        android:id="@+id/button_entrant_cancel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cancel" />

</LinearLayout>
