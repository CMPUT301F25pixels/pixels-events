<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fragment_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_bg"
    android:padding="32dp">

    <TextView
        android:id="@+id/text_signin_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sign In"
        android:textColor="@color/white"
        android:textSize="32sp"
        android:textStyle="bold"
        android:fontFamily="@font/poppins_bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="80dp" />

    <EditText
        android:id="@+id/login_user_email"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:hint="Email address"
        android:textColorHint="@color/text_secondary"
        android:textColor="@color/white"
        android:background="@drawable/view_rounded_text"
        android:padding="16dp"
        android:inputType="textEmailAddress"
        android:fontFamily="@font/poppins"
        app:layout_constraintTop_toBottomOf="@id/text_signin_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

    <EditText
        android:id="@+id/login_user_code"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:hint="Secret Code (Optional)"
        android:textColorHint="@color/text_secondary"
        android:textColor="@color/white"
        android:background="@drawable/view_rounded_text"
        android:padding="16dp"
        android:inputType="text"
        android:fontFamily="@font/poppins"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/login_user_email"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

    <TextView
        android:id="@+id/text_login_role"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Role"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:fontFamily="@font/poppins"
        app:layout_constraintTop_toBottomOf="@id/login_user_code"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp" />

    <com.google.android.material.button.MaterialButtonToggleGroup
        android:id="@+id/login_user_role"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:singleSelection="true"
        app:selectionRequired="true"
        app:checkedButton="@id/login_role_entrant"
        app:layout_constraintTop_toBottomOf="@id/text_login_role"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/login_role_entrant"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:padding="1dp"
            app:backgroundTint="@color/chip_background_color"
            android:text="Entrant"
            android:hint="user"
            android:textColor="@color/white"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/login_role_organizer"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:padding="1dp"
            android:layout_weight="1"
            app:backgroundTint="@color/chip_background_color"
            android:text="Organizer"
            android:hint="user"
            android:textColor="@color/white"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/login_role_admin"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="0dp"
            android:padding="1dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            app:backgroundTint="@color/chip_background_color"
            android:text="Admin"
            android:hint="user"
            android:textColor="@color/white"/>

    </com.google.android.material.button.MaterialButtonToggleGroup>


    <Button
        android:id="@+id/login_user_save"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:text="Sign In"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:backgroundTint="@color/accent_blue"
        android:fontFamily="@font/poppins_bold"
        app:layout_constraintTop_toBottomOf="@id/login_user_role"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp" />

    <ProgressBar
        android:id="@+id/loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/login_user_save"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

    <LinearLayout
        android:id="@+id/login_layout_signup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/login_user_save"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="If you do not have an account "
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:fontFamily="@font/poppins" />

        <TextView
            android:id="@+id/login_user_signup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sign up"
            android:textColor="@color/accent_blue"
            android:textSize="14sp"
            android:textStyle="bold"
            android:fontFamily="@font/poppins_bold"
            android:clickable="true"
            android:focusable="true"
            android:padding="4dp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
