<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Pixelevents" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Pixelevents" parent="Base.Theme.Pixelevents" />

    <!-- BottomSheet dialog overlay for registration period picker -->
    <style name="ThemeOverlay.Pixelevents.BottomSheetDialog" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <style name="Theme.Pixelevents.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Ensure the window shows the gradient immediately to avoid white flash -->
        <item name="android:windowBackground">@drawable/bg_gradient</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>